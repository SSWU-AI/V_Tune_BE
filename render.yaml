services:
  - type: web
    name: v-tune
    env: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: gunicorn v_tune_project.wsgi:application
